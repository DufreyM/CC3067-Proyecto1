node_modules/
dist/
build/
.env
*.log
**/logs/*.jsonl
.DS_Store
.wrangler/
.dev.vars

# Sandbox used by the Filesystem/Git MCP servers - bootstrapped automatically at
# startup (see workspaceBootstrap.ts), including its own nested .git repo.
chatbot/workspace/

# Personal, changes per demo - add classmates' servers here without touching code.
# See mcp-servers.local.example.json for the format.
mcp-servers.local.json

# Where classmates' repos get cloned for ad-hoc testing (see README).
external-mcp-servers/

# Personal dry-run notes for a specific presentation, not part of the deliverable.
docs/presentacion.md
